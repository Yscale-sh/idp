# SA the build pods run as (no cluster powers — they only clone, build, push).
# Name `image-builder` is the internal/builder Job-template contract.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: image-builder
  labels:
    {{- include "image-builder.labels" . | nindent 4 }}
